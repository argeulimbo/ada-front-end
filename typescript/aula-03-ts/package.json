{
  "name": "aula-03-ts",
  "version": "1.0.0",
  "description": "1. Inicializar um projeto Node.js # npm init 2. Instalar o Typescript   2.1. Global: `npm install -g typescript`   2.2. Local: `npm install typescript -D` 3. Utilizar o Typescript instalado para transpilar o código (converter TS em JS): `npx tsc index.ts`",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
